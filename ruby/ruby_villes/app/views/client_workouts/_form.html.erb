<%= form_for(client_workout) do |f| %>
  <% if client_workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client_workout.errors.count, "error") %> prohibited this client_workout from being saved:</h2>

      <ul>
      <% client_workout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :client_name %>
    <%= f.text_field :client_name %>
  </div>

  <div class="field">
    <%= f.label :tainer %>
    <%= f.text_field :tainer %>
  </div>

  <div class="field">
    <%= f.label :duration_mins %>
    <%= f.number_field :duration_mins %>
  </div>

  <div class="field">
    <%= f.label :date_of_workout %>
    <%= f.date_select :date_of_workout %>
  </div>

  <div class="field">
    <%= f.label :paid_amount %>
    <%= f.text_field :paid_amount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
